<Window x:Class="vCam.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="vCam - Виртуальная Камера" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" Height="40">
            <Button Name="StartButton" 
                    Content="▶ Старт" 
                    Width="80" 
                    Height="30" 
                    Margin="5,0"
                    Click="StartButton_Click"/>
            <Button Name="StopButton" 
                    Content="⏹ Стоп" 
                    Width="80" 
                    Height="30" 
                    Margin="5,0"
                    Click="StopButton_Click"
                    IsEnabled="False"/>
            <Button Name="SingleFrameButton" 
                    Content="📸 Один кадр" 
                    Width="100" 
                    Height="30" 
                    Margin="5,0"
                    Click="SingleFrameButton_Click"/>
            <Separator Margin="10,0"/>
            <Button Name="InitVCamButton" 
                    Content="🎥 Инициализировать камеру" 
                    Width="150" 
                    Height="30" 
                    Margin="5,0"
                    Click="InitVCamButton_Click"/>
            <Button Name="StartVCamButton" 
                    Content="📡 Подключиться к VCam" 
                    Width="140" 
                    Height="30" 
                    Margin="5,0"
                    Click="StartVCamButton_Click"
                    IsEnabled="False"/>
            
            <Button Name="DebugVCamButton" 
                    Content="🔍 Отладка VCam" 
                    Width="120" 
                    Height="30" 
                    Margin="5,0"
                    Click="DebugVCamButton_Click"
                    Background="Orange"/>
            <Separator Margin="10,0"/>
            <TextBlock Text="FPS:" 
                      VerticalAlignment="Center" 
                      Margin="5,0"/>
            <ComboBox Name="FpsComboBox" 
                     Width="60" 
                     Height="25"
                     SelectedIndex="1">
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="15"/>
                <ComboBoxItem Content="30"/>
            </ComboBox>
        </StackPanel>
        
        <!-- Область отображения изображения -->
        <Border Grid.Row="1" 
                BorderBrush="Gray" 
                BorderThickness="2" 
                Margin="10">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                         VerticalScrollBarVisibility="Auto">
                <Image Name="DisplayImage" 
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </ScrollViewer>
        </Border>
        
        <!-- Статусная строка -->
        <StatusBar Grid.Row="2" Height="25">
            <StatusBarItem>
                <TextBlock Name="StatusText" Text="Готов к работе"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="FrameCountText" Text="Кадров: 0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="ResolutionText" Text="Разрешение: 640x480"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="VCamStatusText" Text="VCam: Не инициализирована"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window> 